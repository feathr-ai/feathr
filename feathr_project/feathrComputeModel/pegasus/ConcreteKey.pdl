// LINT_SUPPRESS: namespace.three.parts

namespace com.linkedin.frame.compute

record ConcreteKey {
  // Most of the time, this should point to a CONTEXT SOURCE node, e.g. a key in the context called memberId.
  // The main exception would be for a Lookup feature, in which case it would point to another node where the lookup
  // key gets computed.
  key: array[NodeId]

  // It might make sense to add time parameter info here in the future.
}