package com.linkedin.feathr.config.featureanchor.builder.key;

import com.linkedin.feathr.featureDataModel.KeyPlaceholderArray;


/**
 * This interface serves as the generic builder for feature KeyPlaceholderArray.
 */
public interface KeyPlaceholdersBuilder {
  /**
   * Build KeyPlaceholderArray.
   */
  KeyPlaceholderArray build();
}
