apply plugin: 'pegasus'

dependencies {
  implementation spec.product.pegasus.data
  dataModel project(path: ':fr-java-data-models', configuration: 'dataTemplate')
}

afterEvaluate {
    dependencies {
      dataTemplateCompile spec.product.pegasus.data
    }
}